starlette<1.0  # PINNED 2026-08-05: Streamlit Cloud resolved starlette==1.4.0
                # (floating, no prior constraint), whose GZipResponder no longer
                # matches the signature Streamlit 1.61.0's bundled
                # starlette_gzip_middleware.py calls -> TypeError: missing
                # 'thread_minimum_size' -> app 500s on every health check before
                # any app code runs. Constrain below the breaking major bump.
                # Re-test before removing this pin.

streamlit>=1.35.0
yfinance==0.2.66  # PINNED 2026-07-15: was >=0.2.40 (floating), which silently
                  # resolved to 1.5.1 on the last reinstall and coincided with
                  # backtest fetches hanging/never completing. 0.2.66 is the
                  # last 0.2.x release before the 1.0 jump. Re-test before
                  # bumping past this — see utils/backtest_engine.py PERF notes.
pandas==2.2.3
numpy==1.26.4
plotly>=5.22.0
psycopg2-binary>=2.9.9  # CHANGED 2026-08: Neon/Postgres client, replaces supabase-py
python-dotenv>=1.0.0
requests>=2.31.0
numba==0.59.1
scipy
openai>=1.40.0
pyarrow>=15.0.0  # ADDED 2026-07-16: parquet read/write for utils/history_store.py
feedparser>=6.0.0  # ADDED 2026-07-17: RSS ingestion for utils/news_feed.py (ET / Moneycontrol)
psutil>=5.9.0  # ADDED 2026-07-27: RAM/CPU checks for utils/scan_health_monitor.py
                # (self-protecting live_scanner in-process loop). Cross-platform
                # (Windows/Mac/Linux) — deliberately not stdlib `resource`, which
                # is POSIX-only and unusable for local dev/testing off-Linux.
